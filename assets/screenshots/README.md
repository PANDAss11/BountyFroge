# Screenshots

This directory holds interface and terminal screenshots referenced from the main README (Burp Suite extension output, nuclei scan results, report template previews). Add PNG or SVG captures here using the naming convention `feature-name.png`.
